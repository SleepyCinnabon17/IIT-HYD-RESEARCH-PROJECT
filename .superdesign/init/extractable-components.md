# Extractable components
No shared custom layout components exist. RiskIndicator (app.py risk_badge) is a basic component with risk and detected props. Native Gradio primitives need no canvas extraction.
